# devspace.yaml configuration for SpringConfigServer
version: v1beta11

images:
  app:
    image: springconfigserver

deployments:
  - name: spring-config-server
    kubectl:
      manifests:
        - k8s/

dev:
  ports:
    - imageSelector: "#app"
      forward:
        - port: 8888

  sync:
    - imageSelector: "#app"
      localSubPath: .
      containerPath: /app
      excludePaths:
        - .git
        - build
        - .gradle

  terminal:
    imageSelector: "#app"
    command:
      - "./gradlew"
      - "bootRun"
