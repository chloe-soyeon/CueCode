# devspace.yaml configuration for UserService
version: v1beta11

images:
  app:
    image: userservice

deployments:
  - name: user-service
    kubectl:
      manifests:
        - k8s/

dev:
  ports:
    - imageSelector: "#app"
      forward:
        - port: 8081

  sync:
    - imageSelector: "#app"
      localSubPath: .
      containerPath: /app
      excludePaths:
        - .git
        - build
        - .gradle

  terminal:
    imageSelector: "#app"
    command:
      - "./gradlew"
      - "bootRun"
